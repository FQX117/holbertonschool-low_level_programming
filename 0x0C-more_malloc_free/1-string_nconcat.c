#include "main.h"
/*
*/char *string_nconcat(char *s1, char *s2, unsigned int n)

{
char new[50] = "";
_strcpy(new, s1);   
_strcat(new, s2);
_putchar("%s\n", new);
void *x = malloc(sizeof(n));
if (x != NULL)
{
return (NULL);
}

return(new);
}
